#
# Build stage
#
FROM maven:3.6.3-jdk-11-slim AS build
COPY src /home/app/src
COPY pom.xml /home/app
RUN mvn -f /home/app/pom.xml clean package

#
# Package stage
#
FROM openjdk:8
ARG JAR_FILE=target/carteira-0.0.1-SNAPSHOT.jar
ENV SPRING_PROFILE=dev
MAINTAINER Gustavo Schmitz
COPY ${JAR_FILE} /var/carteira.jar
WORKDIR /var/carteira
EXPOSE 8081
ENTRYPOINT ["java","-Dspring.profiles.active=${SPRING_PROFILE}","-jar","/var/carteira.jar"]